# Attribution

- **ndnd** (named-data) — the repo command protocol (`SyncJoin`/`SyncLeave`/`BlobFetch`)
  is **byte-compatible** with ndnd's `repo`.
- **a-thieme/repo** (https://github.com/a-thieme/repo) — the distributed
  coordination layer (replication, capacity-aware placement, heartbeat failover)
  in `ndn-repo-cluster` reimplements its feature set, built NDN-natively.
