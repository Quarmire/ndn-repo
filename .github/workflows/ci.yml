name: CI — ndn-repo

# Depends on ndn-rs + ndn-ext via ../ path deps → sibling checkout.

on:
  push:
    branches: [main]
    paths: ["crates/**", "Cargo.toml", ".github/workflows/ci.yml"]
  pull_request:
    branches: [main]
    paths: ["crates/**", "Cargo.toml", ".github/workflows/ci.yml"]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  core:
    name: repo (clippy / test / doc)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with: { path: ndn-repo }
      - uses: actions/checkout@v4
        with: { repository: "${{ github.repository_owner }}/ndn-rs", ref: 2f0d8cf5733fbcd5fcbecc66a38c4cbe9887e385, path: ndn-rs } # pinned sibling; bump deliberately
      - uses: actions/checkout@v4
        with: { repository: "${{ github.repository_owner }}/ndn-ext", ref: c3a26aa755a5fce10bad3c391b22cc59f1326ad1, path: ndn-ext } # pinned sibling; bump deliberately
      - uses: dtolnay/rust-toolchain@stable
        with: { components: rustfmt, clippy }
      - uses: Swatinem/rust-cache@v2
        with: { workspaces: ndn-repo }
      - name: Format
        working-directory: ndn-repo
        run: cargo fmt --all -- --check
      - name: Lint
        working-directory: ndn-repo
        run: cargo clippy --all-features -- -D warnings
      - name: Test
        working-directory: ndn-repo
        run: cargo test --all-features
      - name: Doc build
        working-directory: ndn-repo
        run: cargo doc --no-deps
